# Django API origin (no trailing slash). /api is appended automatically.
# Local dev default: http://localhost:8000
# Production default (vite build): https://api.corp.crackthecampus.com
VITE_API_BASE_URL=http://localhost:8000
